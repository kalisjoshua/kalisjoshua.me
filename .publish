#! /bin/bash

DIR=dist

# post build file manipulation
touch $DIR/.nojekyll

# use Git to publish to GitHub Enterprise
git add -f $DIR
git commit -am "adding files for publishing"
git push origin :gh-pages
git subtree push --prefix $DIR origin gh-pages
git reset --hard HEAD^
